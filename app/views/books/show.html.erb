<div>
  <%= flash[:notice] %>
</div>

 <div class="container"> 
 <div class="row"> 
  
   <div class="col-lg-3" style="background-color: white;">
  <div class="flexbox">  
 <div class="flex-item"> 
  <h2>User info</h2>
  <%= image_tag @user.get_profile_image(100,100) %>
 
 <table class="table table-striped">
    <tr>
      <th>name</th>
      <th>introduction</th>
    </tr>
    <tr>
      <td><%= @user.name %></td>
      <td><%= @user.introduction %></td>
    </tr>
  </table>

    <% if @user == current_user %>
    
    <%= link_to edit_user_path(@user), class: "btn btn-outline-secomdary btn-block" do %>
  <i class="fa-solid fa-user-gear"></i>
  <% end %>
    
    <% end %>
   
</div>


  <div class="flex-item"> 
 <h1>New book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %> error prohibited this obj from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @newbook do |f| %>


  <h4>Title</h4></h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>

</div>
</div>
</div>
 
 <div class="col-lg-1">
    </div>
    
    




<!-- ユーザーの投稿一覧 -->
<div class="col-lg-8">

  
<h1>Book detail</h1>

<table class="table table-striped">
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
    <tr>
      <td><%=link_to image_tag @book.user.get_profile_image(100,100) %><%= link_to @book.user.name, user_path(@book.user) %></td>
      <td><%= @book.title %></td>
      <td><%= @book.body %></td>
    </tr>
  </table>
  
 
  

 <% if @book.user == current_user %>
    <%= link_to "Destroy", book_path(@book), method: :delete %>
  <%= link_to "Edit", edit_book_path(@book.id) %>
  <% end %>
  
    </div>
    </div>
    </div>
  
  